# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EvernoteTagHelper' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EvernoteTagHelper
  pod 'Evernote-SDK-Mac', '~> 1.22'

  target 'EvernoteTagHelperTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EvernoteTagHelperUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
